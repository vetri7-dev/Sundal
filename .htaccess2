<IfModule mod_rewrite.c>
    RewriteEngine On

    # Pass existing files in public/ through directly
    RewriteRule ^public/ - [L]

    # Route everything else to Laravel
    RewriteRule ^(.*)$ public/index.php [L,QSA]
</IfModule>

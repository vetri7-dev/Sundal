<IfModule mod_rewrite.c>
    RewriteEngine On

    # Let actual files in public/ be served directly
    RewriteRule ^public/ - [L]

    # Route everything else to public/index.php
    RewriteRule ^(.*)$ public/index.php [L,QSA]
</IfModule>
